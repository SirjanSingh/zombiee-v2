name: survivecity-v2
version: 2.0.0
description: |
  SurviveCity v2 — 5-agent multi-resource zombie env with bite transmission,
  zombie spawn waves, iterated voting (3 rounds), broadcast economy, day/night
  cycle, and per-agent inventory. Strict OpenEnv R1 compliance: deterministic
  rewards in (0.01, 0.99), no LLM judging, deterministic bite RNG.
entry_point: server.app:app
host: 0.0.0.0
port: 7861
endpoints:
  reset: /reset
  step: /step
  state: /state
  health: /health
